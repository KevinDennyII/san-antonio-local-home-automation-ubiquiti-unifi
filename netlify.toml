# Astro site lives in /website — not the repo root.
# Full repo is still available, so prebuild can sync ../sales etc. into website/content.
[build]
  base = "website"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
